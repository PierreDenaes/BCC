{% extends 'base.html.twig' %}

{% block title %}FAQ – Bootcamps Centurio{% endblock %}
{% block meta_description %}
Toutes les réponses aux questions que vous vous posez avant de réserver votre bootcamp : déroulement, sécurité, matériel et plus encore.
{% endblock %}
{% block og_title %}FAQ – Questions fréquentes{% endblock %}
{% block og_description %}
Découvre tout ce qu’il faut savoir avant de te lancer dans l’aventure Bootcamps Centurio. Prépare-toi comme un pro.
{% endblock %}



{% block stylesheets %}
    {{ parent() }}
    <style>
        .faq-container {
            max-width: 900px;
            margin: 0 auto;
        }
        .accordion-button {
            font-weight: bold;
            color: #007bff;
        }
        .accordion-button:focus {
            box-shadow: none;
        }
        .accordion-body {
            background: #f8f9fa;
            border-radius: 5px;
        }
    </style>
{% endblock %}

{% block body %}
<div class="container py-5 faq-container">
    <h1 class="text-center mb-4">📖 FAQ BOOTCAMPS CENTURIO</h1>
    <p class="text-center text-muted">Parce qu’on ne part jamais à l’aventure sans être bien briefé ! 💪</p>

    <div class="accordion" id="faqAccordion">
        {% for faq in faqs %}
            <div class="accordion-item">
                <h2 class="accordion-header" id="heading{{ faq.id }}">
                    <button class="accordion-button {% if loop.index != 1 %}collapsed{% endif %}" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{ faq.id }}" aria-expanded="{% if loop.index == 1 %}true{% else %}false{% endif %}" aria-controls="collapse{{ faq.id }}">
                        {{ faq.question }}
                    </button>
                </h2>
                <div id="collapse{{ faq.id }}" class="accordion-collapse collapse {% if loop.index == 1 %}show{% endif %}" aria-labelledby="heading{{ faq.id }}" data-bs-parent="#faqAccordion">
                    <div class="accordion-body">
                        {% for answer in faq.answers %}
                            <p class="mb-2">✅ {{ answer.content }}</p>
                        {% else %}
                            <p class="text-muted">Aucune réponse disponible.</p>
                        {% endfor %}
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-center text-muted">📭 Aucune question pour le moment.</p>
        {% endfor %}
    </div>
</div>
{% endblock %}