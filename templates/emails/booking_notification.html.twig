<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ 'New Booking Paid'|trans }}</title>
</head>
<body>
    <h1>{{ 'New Booking Paid'|trans }}</h1>
    <p>{{ 'A new booking has been successfully paid.'|trans }}</p>
    <p>{{ 'Booking Details:'|trans }}</p>
    <ul>
        <li>{{ 'User:'|trans }} {{ booking.profile.user.fullName }}</li>
        <li>{{ 'Date:'|trans }} {{ booking.bookAt|date('Y-m-d H:i') }}</li>
        <li>{{ 'Forfait:'|trans }} {{ booking.product.forfait }}</li>
        <li>{{ 'Amount:'|trans }} {{ booking.invoice.amount }}</li>
    </ul>
</body>
</html>
