{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<div id="resetMailSentPage" class="d-flex justify-content-center align-items-center" style="min-height: 88vh; background-color: #000;">
    <div class="p-5 rounded text-white" style="background: rgba(0, 0, 0, 0.6); max-width: 700px; width: 100%;">
        <h1 class="text-center white-stroke mb-4">📧 Email envoyé</h1>

        <p>
            Si un compte correspond à l'adresse email saisie, un lien pour réinitialiser ton mot de passe vient d’être envoyé.
        </p>
        <p>
            Ce lien expirera dans :
            <strong>
                {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
            </strong>.
        </p>
        <p>
            Si tu ne reçois pas d'email, vérifie tes spams ou
            <a href="{{ path('app_forgot_password_request') }}" class="text-info text-decoration-underline">essaie à nouveau</a>.
        </p>
    </div>
</div>
{% endblock %}
